# Homepage V6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild the homepage to a single-offer, single-CTA structure centered on the business audit, removing all multi-offer sections and the free lead magnet from the public site.

**Architecture:** Full rewrite of `app/(site)/page.tsx` and `page.module.css`. Nav and Footer simplified to wordmark + one action. Two new section components (ProblemSection, AuditSection). Existing FounderSection and HomeFaq updated in-place. LeadMagnetGate removed from the homepage (file kept, not linked). All CTAs route to `#apply`.

**Tech Stack:** Next.js 14 App Router, CSS Modules, TypeScript. Barlow font, weights 400 and 500 only. No border-radius anywhere. No em dashes. No forbidden words.

---

## File Map

| File | Change |
|---|---|
| `components/Nav.tsx` | Flip wordmark colors, remove nav links, CTA → `/#apply` |
| `components/Nav.module.css` | Update logoSmb/logoAuto colors |
| `components/Footer.tsx` | Simplify: wordmark + copyright + About/Contact links |
| `components/Footer.module.css` | Rewrite for minimal footer |
| `components/ProblemSection.tsx` | NEW — "THE PROBLEM" section |
| `components/ProblemSection.module.css` | NEW |
| `components/AuditSection.tsx` | NEW — "THE APPROACH" section with numbered list |
| `components/AuditSection.module.css` | NEW |
| `components/FounderSection.tsx` | Eyebrow → "WHY ME", name 24px/400, copy tweak, signature uppercase |
| `components/FounderSection.module.css` | Update `.name` and `.signature` styles |
| `components/HomeFaq.tsx` | Replace 7 Q&A with 5 Q&A, update FAQPage schema |
| `app/(site)/page.tsx` | Full rewrite: 8-section V6 structure |
| `app/(site)/page.module.css` | Full rewrite: only V6 classes |
| `app/layout.tsx` | Update metadata title and description |

---

## Task 1: Nav — wordmark flip, remove links, CTA to `/#apply`

**Files:**
- Modify: `components/Nav.tsx`
- Modify: `components/Nav.module.css`

The spec wordmark is "SMB" (ink) + "automation" (blue, lowercase). Current is "SMB" (blue) + "AUTOMATION" (ink). Nav links (Services/Work/Blog) are removed. CTA changes from Calendly external link to `/#apply` internal anchor.

- [ ] **Step 1: Read both files**

Read `components/Nav.tsx` and `components/Nav.module.css`.

- [ ] **Step 2: Rewrite `components/Nav.tsx`**

Replace entire file with:

```tsx
'use client';

import Link from 'next/link';
import { useState, useEffect } from 'react';
import s from './Nav.module.css';

export default function Nav() {
  const [scrolled, setScrolled] = useState(false);
  const [open, setOpen] = useState(false);

  useEffect(() => {
    const handler = () => setScrolled(window.scrollY > 8);
    window.addEventListener('scroll', handler, { passive: true });
    return () => window.removeEventListener('scroll', handler);
  }, []);

  return (
    <>
      <nav className={`${s.nav} ${scrolled ? s.navScrolled : ''}`}>
        <Link href="/" className={s.logo}>
          <span className={s.logoSmb}>SMB</span>
          <span className={s.logoAuto}>automation</span>
        </Link>

        <a href="/#apply" className={s.cta}>
          Get audit →
        </a>

        <button
          className={s.hamburger}
          onClick={() => setOpen(v => !v)}
          aria-label="Toggle menu"
        >
          <span />
          <span />
          <span />
        </button>
      </nav>

      <div className={`${s.drawer} ${open ? s.drawerOpen : ''}`}>
        <a href="/#apply" className={s.drawerCta} onClick={() => setOpen(false)}>
          Get audit →
        </a>
      </div>
    </>
  );
}
```

- [ ] **Step 3: Update `components/Nav.module.css`**

Change `.logoSmb` to ink and `.logoAuto` to blue:

```css
.logoSmb {
  color: var(--ink);
}

.logoAuto {
  color: var(--blue);
}
```

Also remove the `.links` and related rules since the nav links are gone. Replace the entire file with:

```css
.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 5vw;
  height: 64px;
  background: rgba(237,241,247,0.96);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid transparent;
  transition: border-color 0.3s;
}

.navScrolled {
  border-bottom-color: var(--border);
}

.logo {
  font-size: 1rem;
  font-weight: 700;
  letter-spacing: 0.07em;
  text-decoration: none;
}

.logoSmb {
  color: var(--ink);
  text-transform: uppercase;
}

.logoAuto {
  color: var(--blue);
  text-transform: none;
}

.cta {
  font-size: 0.72rem;
  font-weight: 500;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--orange);
  text-decoration: none;
  transition: opacity 0.15s;
}

.cta:hover {
  opacity: 0.75;
}

.hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  background: none;
  border: none;
  padding: 4px;
}

.hamburger span {
  display: block;
  width: 22px;
  height: 2px;
  background: var(--ink);
  transition: transform 0.2s, opacity 0.2s;
}

.drawer {
  display: none;
  position: fixed;
  top: 64px;
  left: 0;
  right: 0;
  background: var(--bg-white);
  border-bottom: 1px solid var(--border);
  padding: 1.5rem 5vw 2rem;
  z-index: 99;
  flex-direction: column;
  gap: 0;
}

.drawerOpen {
  display: flex;
}

.drawerCta {
  display: block;
  text-align: center;
  font-size: 0.78rem;
  font-weight: 500;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--orange);
  padding: 1rem 0;
  text-decoration: none;
  border-top: 1px solid var(--border);
}

@media (max-width: 767px) {
  .cta {
    display: none;
  }
  .hamburger {
    display: flex;
  }
}
```

- [ ] **Step 4: Build check**

```bash
cd /Users/jessebender/smbautomation && npx tsc --noEmit 2>&1 | head -20
```
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add components/Nav.tsx components/Nav.module.css
git commit -m "feat: simplify nav — wordmark flip, remove links, CTA to apply form"
```

---

## Task 2: Footer — simplify to wordmark + copyright + About/Contact

**Files:**
- Modify: `components/Footer.tsx`
- Modify: `components/Footer.module.css`

V6 footer: wordmark (same colors as nav), "© 2026 SMBautomation" copyright, About + Contact nav links. No Calendly, no tagline, no email, no location.

- [ ] **Step 1: Read both files**

Read `components/Footer.tsx` and `components/Footer.module.css`.

- [ ] **Step 2: Rewrite `components/Footer.tsx`**

```tsx
import Link from 'next/link';
import s from './Footer.module.css';

export default function Footer() {
  return (
    <footer className={s.footer}>
      <div className={s.inner}>
        <Link href="/" className={s.logo}>
          <span className={s.logoSmb}>SMB</span>
          <span className={s.logoAuto}>automation</span>
        </Link>
        <ul className={s.links}>
          <li><Link href="/about">About</Link></li>
          <li><Link href="/contact">Contact</Link></li>
        </ul>
        <p className={s.copy}>© 2026 SMBautomation</p>
      </div>
    </footer>
  );
}
```

- [ ] **Step 3: Rewrite `components/Footer.module.css`**

```css
.footer {
  background: var(--dark);
  border-top: 1px solid rgba(255,255,255,0.06);
  padding: 2rem 5vw;
}

.inner {
  max-width: 1100px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 1rem;
}

.logo {
  font-size: 1rem;
  font-weight: 700;
  letter-spacing: 0.07em;
  text-decoration: none;
}

.logoSmb {
  color: rgba(255,255,255,0.6);
  text-transform: uppercase;
}

.logoAuto {
  color: var(--blue);
  text-transform: none;
}

.links {
  list-style: none;
  display: flex;
  gap: 2rem;
}

.links a {
  font-size: 0.75rem;
  font-weight: 500;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.35);
  text-decoration: none;
  transition: color 0.15s;
}

.links a:hover {
  color: rgba(255,255,255,0.75);
}

.copy {
  font-size: 0.72rem;
  color: rgba(255,255,255,0.2);
  letter-spacing: 0.04em;
}

@media (max-width: 580px) {
  .inner {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.75rem;
  }
}
```

- [ ] **Step 4: Build check**

```bash
npx tsc --noEmit 2>&1 | head -20
```
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add components/Footer.tsx components/Footer.module.css
git commit -m "feat: simplify footer — wordmark, copyright, About/Contact only"
```

---

## Task 3: ProblemSection component

**Files:**
- Create: `components/ProblemSection.tsx`
- Create: `components/ProblemSection.module.css`

White background, eyebrow "THE PROBLEM", H2, body paragraph. No CTA.

- [ ] **Step 1: Create `components/ProblemSection.module.css`**

```css
.section {
  background: var(--bg-white);
  padding: 5rem 5vw;
  border-bottom: 1px solid var(--border);
}

.inner {
  max-width: 680px;
  margin: 0 auto;
}

.headline {
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  font-weight: 400;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin: 0.5rem 0 1.25rem;
  line-height: 1.15;
}

.body {
  font-size: 1rem;
  color: var(--ink-mid);
  line-height: 1.75;
}
```

- [ ] **Step 2: Create `components/ProblemSection.tsx`**

```tsx
import s from './ProblemSection.module.css';

export default function ProblemSection() {
  return (
    <section className={s.section}>
      <div className={s.inner}>
        <span className="eyebrow">THE PROBLEM</span>
        <h2 className={s.headline}>Most people we talk to don&apos;t know where to start with AI.</h2>
        <p className={s.body}>
          We partner with you to understand your day to day and find the biggest opportunities where AI, custom software, or off-the-shelf solutions can be implemented to save time and make more money.
        </p>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Build check**

```bash
npx tsc --noEmit 2>&1 | head -20
```
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add components/ProblemSection.tsx components/ProblemSection.module.css
git commit -m "feat: add ProblemSection component"
```

---

## Task 4: AuditSection component

**Files:**
- Create: `components/AuditSection.tsx`
- Create: `components/AuditSection.module.css`

Alt background (`--bg`), eyebrow "THE APPROACH", H2 "The business audit.", body, numbered list (3 items, blue square 24px markers, white digit, weight 500), CTA "Get your audit →" → `#apply`.

- [ ] **Step 1: Create `components/AuditSection.module.css`**

```css
.section {
  background: var(--bg);
  padding: 5rem 5vw;
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

.inner {
  max-width: 680px;
  margin: 0 auto;
}

.headline {
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  font-weight: 400;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin: 0.5rem 0 1.25rem;
  line-height: 1.15;
}

.body {
  font-size: 1rem;
  color: var(--ink-mid);
  line-height: 1.75;
  margin-bottom: 2.5rem;
}

.list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
  margin-bottom: 2.5rem;
}

.item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
}

.num {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 24px;
  height: 24px;
  min-width: 24px;
  background: var(--blue);
  color: #fff;
  font-size: 0.72rem;
  font-weight: 500;
  flex-shrink: 0;
  margin-top: 0.1rem;
}

.itemText {
  font-size: 0.95rem;
  color: var(--ink);
  line-height: 1.6;
}
```

- [ ] **Step 2: Create `components/AuditSection.tsx`**

```tsx
import s from './AuditSection.module.css';

const ITEMS = [
  'A documented look at every workflow that costs you time or money.',
  'ROI scoring on each opportunity, with dollar projections.',
  'A prioritized 90-day plan, scoped to your size and budget.',
];

export default function AuditSection() {
  return (
    <section className={s.section}>
      <div className={s.inner}>
        <span className="eyebrow">THE APPROACH</span>
        <h2 className={s.headline}>The business audit.</h2>
        <p className={s.body}>
          A deep look at your operation. We walk your workflows with you, score every bottleneck by ROI, and hand you a prioritized plan with the dollar impact of each fix. The audit is yours whether or not we build from it.
        </p>
        <ol className={s.list}>
          {ITEMS.map((text, i) => (
            <li key={i} className={s.item}>
              <span className={s.num}>{i + 1}</span>
              <span className={s.itemText}>{text}</span>
            </li>
          ))}
        </ol>
        <a href="#apply" className="btn-orange">Get your audit →</a>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Build check**

```bash
npx tsc --noEmit 2>&1 | head -20
```
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add components/AuditSection.tsx components/AuditSection.module.css
git commit -m "feat: add AuditSection component with numbered list and CTA"
```

---

## Task 5: FounderSection V6 — eyebrow, name size, copy, signature

**Files:**
- Modify: `components/FounderSection.tsx`
- Modify: `components/FounderSection.module.css`

Changes from V4: eyebrow "WHY ME" (was "THE FOUNDER"), name 24px/weight 400 (was 1rem/600), para 1 starts "I have 13 years..." (remove "I'm Jesse. "), signature uppercase with letter-spacing.

- [ ] **Step 1: Read both files**

Read `components/FounderSection.tsx` and `components/FounderSection.module.css`.

- [ ] **Step 2: Rewrite `components/FounderSection.tsx`**

```tsx
import Image from 'next/image';
import s from './FounderSection.module.css';

export default function FounderSection() {
  return (
    <section className={s.founder}>
      <div className={s.inner}>
        <div className={s.photoBlock}>
          <Image
            src="/assets/jesse-bender.png"
            alt="Jesse Bender, Founder of SMBautomation"
            fill
            sizes="(max-width: 767px) 100vw, (max-width: 1099px) 50vw, 440px"
            style={{ objectFit: 'cover', objectPosition: 'top' }}
          />
        </div>
        <div className={s.copy}>
          <span className="eyebrow">WHY ME</span>
          <p className={s.name}>Jesse Bender</p>
          <p className={s.body}>
            I have 13 years of experience starting and scaling companies, with an exit via Fortune 500 acquisition. My recent wins include scaling a health tech startup from $24K ARR to over $25M ARR. I&apos;ve scaled teams from 1 to 11, implemented CRMs and operations tools across multiple companies, owned a restaurant during the pandemic, and managed a $200M real estate portfolio. Through it all, my focus has stayed on efficiency and customer experience. I bring both into SMBautomation, where I help businesses scale without adding headcount.
          </p>
          <p className={s.body}>
            SMBautomation specializes in system building, implementing new technology, AI workflows, and custom application builds to scale operations. We primarily work in property management, healthcare, hospitality, home building, and local service businesses to grow without bloating overhead. AI just collapsed the cost of building real software. The owners who move now keep the gains.{' '}
            <a href="#apply" className={s.ctaLink}>Get in touch today</a> to see how we can streamline your business, help you make more money, and save you time.
          </p>
          <p className={s.signature}>JESSE BENDER, FOUNDER</p>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Update `components/FounderSection.module.css`**

Replace the `.name` and `.signature` rules. Full updated file:

```css
/* ── Founder Section ─────────────────── */
.founder {
  background: var(--bg-white);
  padding: 5rem 5vw;
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

.inner {
  max-width: 1100px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 2fr 3fr;
  gap: 4rem;
  align-items: start;
}

.photoBlock {
  aspect-ratio: 4 / 5;
  position: relative;
  overflow: hidden;
}

.copy {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

.name {
  font-size: 1.5rem;
  font-weight: 400;
  color: var(--ink);
  letter-spacing: -0.01em;
  margin: 0;
}

.body {
  font-size: 0.95rem;
  color: var(--ink-mid);
  line-height: 1.7;
  margin: 0;
}

.ctaLink {
  color: var(--blue);
  text-decoration: underline;
  text-underline-offset: 3px;
  transition: color 0.15s;
}
.ctaLink:hover {
  color: var(--blue-deep);
}

.signature {
  font-size: 0.68rem;
  font-weight: 600;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--ink-mid);
  margin: 0;
}

@media (max-width: 1099px) {
  .inner {
    grid-template-columns: 1fr 1fr;
    gap: 2.5rem;
  }
}

@media (max-width: 767px) {
  .inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .photoBlock {
    aspect-ratio: 3 / 2;
  }
}
```

- [ ] **Step 4: Build check**

```bash
npx tsc --noEmit 2>&1 | head -20
```
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add components/FounderSection.tsx components/FounderSection.module.css
git commit -m "feat: founder section V6 — eyebrow WHY ME, name 24px, uppercase signature"
```

---

## Task 6: HomeFaq — 5 questions, updated FAQPage schema

**Files:**
- Modify: `components/HomeFaq.tsx`

Replace the 7 FAQS with the 5 V6 questions. Update the `faqSchema` object accordingly. CSS module is unchanged.

- [ ] **Step 1: Read `components/HomeFaq.tsx`**

- [ ] **Step 2: Replace the FAQS array and faqSchema in `components/HomeFaq.tsx`**

Replace the entire file with:

```tsx
'use client';

import { useState } from 'react';
import Script from 'next/script';
import s from './HomeFaq.module.css';

const FAQS = [
  {
    q: 'What kind of businesses do you work with?',
    a: 'Owners in property management, healthcare, hospitality, home building, and local service businesses. The pattern is the same: real revenue, real customers, ops held together by spreadsheets and the owner\'s memory.',
  },
  {
    q: 'How is this different from hiring a developer?',
    a: 'Developers build what you tell them to build. We figure out what to build first. The audit proves the ROI before any code gets written.',
  },
  {
    q: 'What does the audit cost?',
    a: 'Scoped to your operation. We send specifics after a 30-minute conversation. If we build, the audit fee applies to the first build.',
  },
  {
    q: 'How fast do you ship?',
    a: 'First audit deliverable in 2 weeks. Most builds ship in 4 to 8 weeks after that.',
  },
  {
    q: 'What if it doesn\'t work?',
    a: 'Every build is scoped from a proven ROI number in the audit. If we can\'t show the math, we don\'t take the work.',
  },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: FAQS.map(f => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: {
      '@type': 'Answer',
      text: f.a,
    },
  })),
};

export default function HomeFaq() {
  const [open, setOpen] = useState<number | null>(null);

  return (
    <section className={s.section} id="faq">
      <Script
        id="faq-schema"
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema) }}
      />
      <div className={s.inner}>
        <div className={s.header}>
          <span className="eyebrow">QUESTIONS</span>
          <h2 className={s.headline}>What owners ask first.</h2>
        </div>
        <div className={s.list}>
          {FAQS.map((faq, i) => (
            <div key={i} className={s.item}>
              <button
                className={s.question}
                onClick={() => setOpen(open === i ? null : i)}
                aria-expanded={open === i}
              >
                <span>{faq.q}</span>
                <span className={`${s.chevron}${open === i ? ` ${s.chevronOpen}` : ''}`}>▼</span>
              </button>
              {open === i && (
                <p className={s.answer}>{faq.a}</p>
              )}
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Build check**

```bash
npx tsc --noEmit 2>&1 | head -20
```
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add components/HomeFaq.tsx
git commit -m "feat: HomeFaq V6 — 5 questions, updated FAQPage schema"
```

---

## Task 7: Homepage page.tsx + page.module.css full rewrite

**Files:**
- Modify: `app/(site)/page.tsx`
- Modify: `app/(site)/page.module.css`

Eight sections: Hero, Problem, Audit, Founder, FAQ, Final CTA, Application Form. Remove: stats bar, quote row, process, engagement ladder, who section, case studies, lead magnet. Hero background is white (`--bg-white`), single column, single CTA.

- [ ] **Step 1: Read both files**

Read `app/(site)/page.tsx` and `app/(site)/page.module.css`.

- [ ] **Step 2: Rewrite `app/(site)/page.tsx`**

```tsx
import type { Metadata } from 'next';
import ProblemSection from '@/components/ProblemSection';
import AuditSection from '@/components/AuditSection';
import FounderSection from '@/components/FounderSection';
import HomeFaq from '@/components/HomeFaq';
import ApplicationForm from '@/components/ApplicationForm';
import s from './page.module.css';

export const metadata: Metadata = {
  title: { absolute: 'SMBautomation - Business Audits and AI Builds for Small and Mid-Size Businesses | Jesse Bender, Founder' },
  description: 'Get a business audit from Jesse Bender: 13 years scaling companies, $200M portfolio managed, Fortune 500 exit. We find where time and money are leaking, then build only what pays back.',
  alternates: { canonical: 'https://smbautomation.io' },
};

export default function HomePage() {
  return (
    <>
      {/* 1. Hero */}
      <section className={s.hero}>
        <div className={s.heroInner}>
          <span className="eyebrow">STRATEGIC GROWTH PARTNER FOR SMBS</span>
          <h1 className={s.heroHeadline}>
            Stop guessing where automation <em>pays off.</em>
          </h1>
          <p className={s.heroSub}>
            It starts with a business audit. We find where time and money are leaking, then we build only what pays back.
          </p>
          <a href="#apply" className="btn-orange">Get your audit →</a>
          <p className={s.heroSupport}>Tell us about your business. We respond within 1 business day.</p>
        </div>
      </section>

      {/* 2. Problem */}
      <ProblemSection />

      {/* 3. The Audit */}
      <AuditSection />

      {/* 4. Founder */}
      <FounderSection />

      {/* 5. FAQ */}
      <HomeFaq />

      {/* 6. Final CTA */}
      <section className={s.finalCta}>
        <div className={s.finalCtaInner}>
          <span className="eyebrow-light">READY</span>
          <h2 className={s.finalCtaHeadline}>Find out what&apos;s actually worth automating.</h2>
          <p className={s.finalCtaSub}>15-minute application. We respond within 1 business day.</p>
          <a href="#apply" className="btn-orange">Get your audit →</a>
        </div>
      </section>

      {/* 7. Application Form */}
      <ApplicationForm />
    </>
  );
}
```

- [ ] **Step 3: Rewrite `app/(site)/page.module.css`**

```css
/* ── Hero ─────────────────────────────── */
.hero {
  background: var(--bg-white);
  padding: 6rem 5vw 5rem;
  border-bottom: 1px solid var(--border);
}

.heroInner {
  max-width: 680px;
  margin: 0 auto;
}

.heroHeadline {
  font-size: clamp(1.875rem, 4vw, 3rem);
  font-weight: 400;
  line-height: 1.1;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin: 0.5rem 0 1.25rem;
}

.heroHeadline em {
  font-style: italic;
  color: var(--blue);
}

.heroSub {
  font-size: 1rem;
  color: var(--ink-mid);
  line-height: 1.75;
  margin-bottom: 2rem;
}

.heroSupport {
  font-size: 0.82rem;
  color: var(--ink-faint);
  margin-top: 1rem;
  line-height: 1.5;
}

/* ── Final CTA ───────────────────────── */
.finalCta {
  background: var(--dark);
  padding: 5rem 5vw;
}

.finalCtaInner {
  max-width: 680px;
  margin: 0 auto;
}

.finalCtaHeadline {
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  font-weight: 400;
  letter-spacing: -0.02em;
  color: #fff;
  margin: 0.5rem 0 1rem;
  line-height: 1.15;
}

.finalCtaSub {
  font-size: 0.95rem;
  color: rgba(255,255,255,0.5);
  line-height: 1.7;
  margin-bottom: 2rem;
}

/* ── Responsive ──────────────────────── */
@media (max-width: 767px) {
  .hero {
    padding: 4rem 5vw 3rem;
  }
  .finalCta {
    padding: 3.5rem 5vw;
  }
}
```

- [ ] **Step 4: Build check**

```bash
npx tsc --noEmit 2>&1 | head -30
```
Expected: no errors. If you see "Module not found" for ProblemSection or AuditSection, verify those files were created in Tasks 3 and 4.

- [ ] **Step 5: Commit**

```bash
git add "app/(site)/page.tsx" "app/(site)/page.module.css"
git commit -m "feat: homepage V6 full rewrite — single offer, single CTA"
```

---

## Task 8: Metadata and schema in layout.tsx

**Files:**
- Modify: `app/layout.tsx`

Update metadata `default` title and `description` to match V6. Update orgSchema description. The Person + Service entries are already present from V4 and remain correct.

- [ ] **Step 1: Read `app/layout.tsx`**

- [ ] **Step 2: Update the metadata object**

Find the `metadata` export and replace `title.default` and `description`:

```ts
export const metadata: Metadata = {
  metadataBase: new URL('https://smbautomation.io'),
  title: {
    template: '%s | SMBautomation',
    default: 'SMBautomation - Business Audits and AI Builds for Small and Mid-Size Businesses | Jesse Bender, Founder',
  },
  description: 'Get a business audit from Jesse Bender: 13 years scaling companies, $200M portfolio managed, Fortune 500 exit. We find where time and money are leaking, then build only what pays back.',
  icons: {
    icon: [
      { url: '/favicon.svg', type: 'image/svg+xml' },
      { url: '/favicon.ico', sizes: '48x48' },
    ],
  },
  openGraph: {
    title: 'SMBautomation - Business Audits and AI Builds for Small and Mid-Size Businesses | Jesse Bender, Founder',
    description: 'Get a business audit from Jesse Bender: 13 years scaling companies, $200M portfolio managed, Fortune 500 exit.',
    url: 'https://smbautomation.io',
    siteName: 'SMBautomation',
    type: 'website',
  },
  alternates: {
    canonical: 'https://smbautomation.io',
  },
};
```

- [ ] **Step 3: Update orgSchema description and name**

In the `orgSchema` object, update:
- `name`: change `'SMB Automation'` → `'SMBautomation'`
- `description`: change to `'Business audits and AI builds for small and mid-size businesses. We find where time and money are leaking, then build only what pays back.'`

- [ ] **Step 4: Build check**

```bash
npx tsc --noEmit 2>&1 | head -20
```
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add app/layout.tsx
git commit -m "feat: update metadata and schema for V6 — audit-led positioning"
```

---

## Self-Review: Spec Coverage Check

| Spec requirement | Task |
|---|---|
| Nav: wordmark SMB (ink) + automation (blue, lowercase) | Task 1 |
| Nav: single "Get audit →" CTA in orange, no other links | Task 1 |
| Footer: About + Contact links, "© 2026 SMBautomation" | Task 2 |
| Hero: eyebrow, H1 with italic blue "pays off.", subhead, single CTA, supporting line | Task 7 |
| Hero: light background, single column | Task 7 |
| Problem section: eyebrow, H2, body | Task 3 |
| Audit section: eyebrow, H2, body, numbered list (3 items, blue squares), CTA | Task 4 |
| Numbered list markers: 24px square, solid blue, white digit, weight 500 | Task 4 |
| Founder: eyebrow "WHY ME" | Task 5 |
| Founder: name 24px weight 400 | Task 5 |
| Founder: para 1 starts "I have 13 years..." (no "I'm Jesse.") | Task 5 |
| Founder: signature uppercase with letter-spacing | Task 5 |
| FAQ: eyebrow "QUESTIONS", H2 "What owners ask first." | Task 6 |
| FAQ: exactly 5 questions | Task 6 |
| FAQ: FAQPage schema updated to 5 questions | Task 6 |
| Final CTA: dark background, eyebrow "READY", H2, subhead, single CTA | Task 7 |
| All CTAs route to #apply | Tasks 1, 4, 7 |
| No em dashes anywhere | All tasks |
| No rounded corners | All tasks |
| No forbidden words | All tasks |
| Metadata title includes Jesse Bender, Founder | Task 8 |
| Schema name updated to "SMBautomation" | Task 8 |
| LeadMagnetGate removed from homepage (file kept) | Task 7 — not imported |
| HeroDiagram removed from homepage | Task 7 — not imported |
| Stats bar, quote row, process, engagement ladder, who, case studies removed | Task 7 |

**Gaps found:** None. All Sprint 1 requirements covered.
